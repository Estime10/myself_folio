/**
 * Configuration globale du site — source unique pour le nom et métadonnées statiques.
 * Utilisé par le header (desktop + mobile) pour éviter la duplication (DRY).
 */
export const SITE_NAME = "Estime Vangu" as const;
